services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    restart: always
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    networks:
      - ai-network

  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    container_name: litellm-router
    restart: always
    ports:
      - "4000:4000"
    volumes:
      - ./litellm-router/config.yaml:/app/config.yaml
    command: ["--config", "/app/config.yaml", "--port", "4000", "--host", "0.0.0.0"]
    environment:
      - LITELLM_MASTER_KEY=sk-jetson-master-key-1234
      - MOONSHOT_API_KEY=${MOONSHOT_API_KEY}
      - GROQ_API_KEY=${GROQ_API_KEY}
    networks:
      - ai-network

  whisper:
    image: dustynv/whisper.cpp:r36.2.0
    container_name: whisper-server
    restart: always
    ports:
      - "8080:8080"
    # Using 'base' model for good balance of speed and accuracy on Orin Nano
    command: ["--model", "base", "--port", "8080"]
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    networks:
      - ai-network

networks:
  ai-network:
    driver: bridge

volumes:
  ollama-data:
